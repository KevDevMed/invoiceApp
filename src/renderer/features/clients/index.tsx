/**
 * Clients feature barrel.
 *
 * OWNER: replace this stub with the real page. Keep the exported name
 * `ClientsPage` — `src/renderer/routes.tsx` is frozen and imports it from here.
 * Add as many files as you like inside this directory.
 */

import { Placeholder } from '../../pages/Placeholder';

export function ClientsPage(): React.JSX.Element {
  return <Placeholder name="Clients" />;
}
